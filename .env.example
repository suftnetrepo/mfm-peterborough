# Jerur backend (read-only data: settings, sliders, events, fellowship, regular services)
JERUR_NEXT_BASE_URL= 
JERUR_NEXT_API_KEY=
# Brevo (transactional email for contact / prayer request / testimony forms)
BREVO_API_KEY=
USER_NAME=
CHURCH_INBOX_EMAIL=
TEAM=

# Public site URL — backs canonical links, Open Graph/Twitter cards, and
# sitemap.xml/robots.txt (see app/layout.tsx, app/sitemap.ts, app/robots.ts).
# Set this to the real production domain once one exists.
NEXT_PUBLIC_SITE_URL=

